import "reflect-metadata";
import React from "react";

import { ApiKeySetup } from "./ApiKeySetup.component";

describe("ApiKeySetup", () => {
  it("should be exported", () => {
    expect(ApiKeySetup).toBeTruthy();
  });
});
