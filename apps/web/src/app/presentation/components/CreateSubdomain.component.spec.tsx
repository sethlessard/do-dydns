import "reflect-metadata";
import React from "react";

import { CreateSubdomain } from "./CreateSubdomain.component";

describe("CreateSubdomain", () => {
  it("should be exported", () => {
    expect(CreateSubdomain).toBeTruthy();
  });
});
