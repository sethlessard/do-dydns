import "reflect-metadata";
import React from "react";

import ReactSettingsView from "./ReactSettingsView";

describe("ReactSettingsView", () => {
  it("should be exported", () => {
    expect(ReactSettingsView).toBeTruthy();
  });
});
