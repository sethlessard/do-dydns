import "reflect-metadata";

import { SettingsViewPresenter } from "./SettingsViewPresenter";

describe("SettingsViewPresenter", () => {
  it("should be exported", () => {
    expect(SettingsViewPresenter).toBeTruthy();
  });
});
