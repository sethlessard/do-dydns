import "reflect-metadata";

import { AppbarPresenter } from "./AppbarPresenter";

describe("AppbarPresenter", () => {
  it("should be exported", () => {
    expect(AppbarPresenter).toBeTruthy();
  });
});
