import "reflect-metadata";

import { LogViewPresenter } from "./LogViewPresenter";

describe("LogViewPresenter", () => {
  it("should be exported", () => {
    expect(LogViewPresenter).toBeTruthy();
  });
});
