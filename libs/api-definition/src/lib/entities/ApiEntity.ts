export interface ApiEntity {
  /**
   * The API entity's ID.
   */
  id: number;

  /**
   * When the API entity was created.
   */
  created: number;

  /**
   * When the API entity was last updated.
   */
  updated: number;
}
